// Tous les <a> à l'intérieur des .navbar__menu
const menuLinks = document.querySelectorAll(".navbar__menu a");

ex:

const slider = document.getElementById("slides-js");
if (!slider) return; // ⚠️ Si le slider n'existe pas, on ne fait rien

const slide__video = slider.querySelectorAll("video"); // <video>
const menuLinks = document.querySelectorAll(".navbar__menu a");

// Tous les inputs de type texte
const inputs = document.querySelectorAll("input[type='text']");
// Premier enfant <video> de chaque slide
const firstVideos = document.querySelectorAll(".slides__slide > video:first-child");