{% extends "@Shared/layout/base.twig" %}

{% block title %}Connexion{% endblock %}

{% block content %}
<form method="post" action="/auth/connection">
    <h2>Connectez-vous</h2>
    <input type="hidden" name="csrf_token" value="{{ csrf_token }}">
    {% if flash_error %}
        <div class="alert alert-danger">{{ flash_error }}</div>
    {% endif %}
    <label for="email">Email</label>
    <input type="email" id="email" name="email" placeholder="email@mail.com" maxlength="30" required autofocus>
    <label for="password">Mot de passe</label>
    <input type="password" id="password" name="password" placeholder="password" maxlength="30" required>
    <button type="submit" class="btn btn-primary">Se connecter</button>
</form>
<form method="post" action="/auth/forgot-password">
    <label for="email">Mot de passe oublié ? </label>
    <input type="email" id="email" name="email" placeholder="email@mail.com" maxlength="30" required autofocus>
    <button type="submit" class="btn btn-primary">Envoyer sur la boite mail</button>
</form>
{% endblock %}